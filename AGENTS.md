# 项目维护说明

本文件适用于整个仓库。修改前阅读 README、相关源码和 `docs/` 中的说明，检查 Git 状态并保留已有改动。

## 当前范围

- 当前版本为 0.6.1，仅支持 Jellyfin 12.0.0，目标框架为 `net10.0`。
- 界面和面向用户的说明使用中文；不要为旧版 Jellyfin 增加兼容层。
- 保持当前评分、筛选、直接编辑评价、显式保存和单击放弃/撤销的行为，避免无关重构。
- 跨客户端评分映射是默认关闭的实验功能，按认证账号和电影库限定范围。物理 Android TV 尚未验收。

## 数据与隐私边界

1. 身份来自 Jellyfin 认证，不信任浏览器提供的用户 ID。
2. 读写及查询均检查媒体可见性，不泄露其他用户的数据或不可见项目。
3. 评分以 Jellyfin `UserData.Rating` 为准；评价存储在插件自己的 SQLite 数据库中。
4. 不修改共享评分、NFO、媒体目录或网页客户端安装文件；跨客户端兼容只替换响应字段。
5. 网页资源内嵌，不调用外部元数据、统计或云同步服务。
6. 不提交账号凭据、令牌、真实媒体或评价、数据库、运行日志、备份及本机部署记录。

## 修改与验证

- 实现修改使用独立的 `codex/` 分支，保留现有用户改动。
- 仅为实际回归风险增加测试；不要删除仍保护当前行为的测试来精简目录。
- 运行 `scripts/verify.ps1`：构建、格式、69 项以上后端测试及资源/程序集检查必须通过。
- 网页交互变化运行 `tests/web/server.cjs`，检查桌面和 360px 页面；设置页单独验证。详见 `docs/TESTING.md`。
- 版本变更同步项目版本、`build.yaml`、打包脚本、网页资源标记和 README。
- 构建、复制 DLL 或合成测试通过不代表真实部署和物理客户端验收。

## 部署与外部操作

- 安装前停止 Jellyfin，并完整备份服务器数据、配置及旧插件；保留可恢复副本。
- 生产 SQLite 操作使用实际服务器运行账号，检查数据库及 WAL/SHM 权限，不删除活动辅助文件。
- 重启后核对最新日志中的实际插件版本，再验证已有数据及可恢复的写入操作。
- 提交、推送、发布、修改可见性或改写历史须有当前任务授权；不要将维护任务自动扩大为发布。
- 历史验收、开发原型和本机运维记录保留在仓库之外，不重新加入公开仓库。
